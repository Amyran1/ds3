feature_set:
  name: population_per_household
  version: v1
  recipe_version: population_per_household_v1
  row_keys:
    - name: row_id
      dtype: int64
  n_rows: 20640
  cache_module: projects.california_housing_demo.features.population_per_household.cache
  local_path: null
  remote_path: null
  local_path_reason: in-memory only; 20640-row vectorized division on sklearn dataset
  source_versions:
    sklearn_california_housing: pinned via project deps
  source_fingerprints:
    sklearn_california_housing: sha256:unpinned-stub
  code_version: git:autoloop-iter-4
  features:
    - name: population_per_household
      dtype: float64
      semantic_type: numeric
      description: >
        Population / AveOccup per census block group. `Population` is the
        block-group total and `AveOccup` is the average household occupancy,
        so the ratio approximates household count per block group — a
        density proxy distinct from raw AveOccup. Brainstorm BS-2026-05-13-pph004.
      null_policy: no nulls expected; sklearn dataset guarantees AveOccup > 0
      leakage_notes: pre-outcome inputs only; no temporal leakage path.
